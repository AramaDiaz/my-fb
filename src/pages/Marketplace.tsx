import { Box } from '@mui/material';

const Marketplace = () => (
  <Box
    display='flex'
    flexDirection='column'
    sx={{
      flex: 1,
      textAlign: 'center',
    }}
  >
    Marketplace
  </Box>
);

export default Marketplace;
