import { Box } from '@mui/material';

const Videos = () => (
  <Box
    display='flex'
    flexDirection='column'
    sx={{
      flex: 1,
      textAlign: 'center',
    }}
  >
    Videos
  </Box>
);

export default Videos;
