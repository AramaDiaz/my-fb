import { Box } from '@mui/material';

const Games = () => (
  <Box
    display='flex'
    flexDirection='column'
    sx={{
      flex: 1,
      textAlign: 'center',
    }}
  >
    Games
  </Box>
);

export default Games;
