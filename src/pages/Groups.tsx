import { Box } from '@mui/material';

const Groups = () => (
  <Box
    display='flex'
    flexDirection='column'
    sx={{
      flex: 1,
      textAlign: 'center',
    }}
  >
    Groups
  </Box>
);

export default Groups;
